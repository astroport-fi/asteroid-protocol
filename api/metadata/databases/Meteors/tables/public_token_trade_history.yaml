table:
  name: token_trade_history
  schema: public
object_relationships:
  - name: token
    using:
      manual_configuration:
        column_mapping:
          token_id: id
        insertion_order: null
        remote_table:
          name: token
          schema: public
  - name: transaction
    using:
      manual_configuration:
        column_mapping:
          transaction_id: id
        insertion_order: null
        remote_table:
          name: transaction
          schema: public
