table:
  name: bridge_history
  schema: public
object_relationships:
  - name: transaction
    using:
      foreign_key_constraint_on: transaction_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - amount
        - receiver
        - signature
        - token_id
        - transaction_id
      filter: {}
    comment: ""
