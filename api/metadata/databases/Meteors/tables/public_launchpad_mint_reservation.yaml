table:
  name: launchpad_mint_reservation
  schema: public
object_relationships:
  - name: launchpad
    using:
      foreign_key_constraint_on: launchpad_id
select_permissions:
  - role: anonymous
    permission:
      columns:
        - address
        - collection_id
        - id
        - launchpad_id
        - stage_id
        - token_id
      filter: {}
    comment: ""
